"""
Data Access module, Module Doc
"""
import sqlite3

# conn =  sqlite3.connect('db.db')
# cursor =  conn.cursor()
# cursor.execute('Select * from ')
# conn.commit()
# conn.close

# Cursor executes the sql not the conn
# cursors move in the results rows
